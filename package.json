{
  "name": "puppeteer-recorder",
  "version": "1.0.0",
  "description": "recording test in headless browser in docker container",
  "main": "recorder.js",
  "scripts": {
    "start": "node recorder.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "storm",
  "license": "MIT",
  "dependencies": {
    "puppeteer": "^18.0.5",
    "puppeteer-screen-recorder": "^2.1.1"
  }
}
